NODE_ENV=development
PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_NAME=tasktracker
DB_USER=postgres
DB_PASSWORD=change_me

SESSION_SECRET=change_me_please_use_a_long_random_value
CORS_ORIGIN=http://localhost:8080
SESSION_TTL_HOURS=24

# Docker compose defaults
# When using docker-compose, DB_HOST should usually be set to db
